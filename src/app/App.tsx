export function App() {
  return (
    <main>
      <h1>Schema Version Control</h1>
      <p>
        Version control for database schemas. The engine lives in{' '}
        <code>src/engine</code> and is pure TypeScript; this app consumes its
        output.
      </p>
    </main>
  );
}
